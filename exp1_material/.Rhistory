d = read.csv('composed.csv', header=F)

head(d)
d = read.csv('composed.csv', header=F)


colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','disnw')

head(d)
d$word==d$distgramm
head(d)
d [c('item','cond'),d$word==d$distgramm]
d [ d$word==d$distgramm, c('item','cond')]
samegramm = d [ d$word==d$distgramm, c('item','cond')]

len(samegramm)
size(samegramm)
length(samegramm)
samegramm = d [ d$word=='erivb', c('item','cond')]

samegramm
length(samegramm)
nrowsamegramm)
nrow(samegramm)
samegramm = d [ d$word==d$distgramm, c('item','cond')]

nrow(samegramm)

if (nrow(samegramm))  {
   warning('some distractors match targets in grammatical condition')
}

if (nrow(samegramm))  {
   warning('some distractors match targets in grammatical condition')
   print (samegramm)
}

sameword = d [ d$word==d$distword, c('item','cond')]

if (nrow(sameword))  {
   warning('some distractors match targets in grammatical condition')
   print (sameword)
}

d$distgramm
trimws('a ')
trimws(' a ')
trimws(d$distgramm)
trimws(d$distgramm) == d$distgramm
 
for (c in col(d)) {
  print(c)
}

columns(d)
colnames(d)
for (c in colnames(d)) {
  print(d[[c]])
}

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

head(d)
len(disgram)
strlen(disgram)
nchar(disgram)
nchar(d$disgramm)
nchar(d$distpseudo)
nchar(d$distpseudo) == d$nl
nchar(d$word) == d$nl
all(nchar(d$word) == d$nl)
samegramm = d [ d$word==d$distgramm, c('item','cond')]

if (nrow(samegramm))  {
   warning('some distractors match targets in grammatical condition')
   print (samegramm)
}

sameword = d [ d$word==d$distword, c('item','cond')]

if (nrow(sameword))  {
   warning('some distractors match targets in word condition')
   print (sameword)
}


for (dty in distnames) {
   chksame = d [ d[['word']]==d[[dty]], c('item','cond')]
   if (nrow(chksame))  {
     warning(paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
}

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   chksame = d [ d[['word']]==d[[dty]], c('item','cond')]
   if (nrow(chksame))  {
     warning(paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
}

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']]==d[[dty]], c('item','cond')]
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
}

diffl = nl == nchar(d[[dty]])

diffl = d[['nl']] == nchar(d[[dty]])

diffl
 d[['nl']] == nchar(d[[dty]])

 d[['nl']] 
nchar(d[[dty]])

d[[dty]]

dty
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')


d[[dty]]

diffl = d[['nl']] == nchar(d[[dty]])

diffl
diffl = d[['nl']] == nchar(d[[dty]])


d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[[nl]] = nchar d[['word']]

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print('some distractor has different number of letters:')
      print d[diffl, c('item', 'cond')]
   }
}

# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[[nl]] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('gironalista')
nchar('direttore')
nchar('presidente')
nchar('presbitero')
nchar('cancelliere')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar(falegname')
'
nchar('falegname')
nchar('dittatore')
nchar('critica')
nchar('odia')
nchar('combatte')
nchar('chiamava')
nchar('vuole')
nchar('voleva')
nchar('ammira')
nchar('rimira')
nchar('detesta')
nchar('pittore')
nchar('nobile')
nchar('presbitero')
nchar('prelato')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('giocatore')
nchar('presbitero')
nchar('architetto')
nchar('militare')
nchar('macellaio')
nchar('armatori')
nchar('amministratori')
nchar('intendtitori')
nchar('alunni')
nchar('elettori')
nchar('ignoranti')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('attraversa')
nchar('conosceva')
nchar('incontrava')
nchar('meccanico')
nchar('ragazza')
nchar('ragazzino')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('prende')
nchar('avvista')
nchar('vedrà')
nchar('rivede')
nchar('rivedrà')
nchar('cercherà')
nchar('scopre')
nchar('scoprì')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('pubblico')
nchar('edificio')
nchar('ostilità')
nchar('piramide')
nchar('finiscimi')
nchar('visitava')
nchar('carezzava')
nchar('coglieva')
nchar('sentiva')
nchar('ascoltava')
nchar('provava')
nchar('chiedeva')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('vitello')
nchar('potersi')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('potersi')
nchar('vitello')
nchar('finanza')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

d[d$item==113,]
dty=gramm
dty='gramm'
diffl = d[['nl']] != nchar(d[[dty]])
diffl
dty='distgramm'
diffl = d[['nl']] != nchar(d[[dty]])
diffl
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('beccinato')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('mercotto')
nchar('piracchia')
nchar('piraccia')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('raterisco')
nchar('Ditagnava')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('sigimente')
nchar('midastro')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('trampollati')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('duhnceijnw')
nchar('uthgnbj.')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('wcergfbhu')
nchar('eaxwrq.')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('rvkbjrvh.')
nchar('buosxq')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('nnchepqaurk.')
nchar('edhtpqak')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('mdsnrhjd')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('ejxqkzvf')
nchar('xgplceuywlIl')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('aicjwcre')
nchar('xxllje')
nchar('tiecfn')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('efcjxoipko')
nchar('xwdliovr.')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('vwygiuh')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('mgepalg')
nchar('acvjejgf.')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('ozuulhq
')
nchar('ozuulhq')
nchar('olwhip')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('dhzwrsqgfne')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

nchar('eirndfit')
nchar('edcjpw
')
nchar('edcjpw')
# read the hand-composed material

d = read.csv('composed.csv', header=F)

colnames(d) = c('item','cond','sentence','wp','word','nl','distgramm','distword','distpseudo','distnw')

# trim whitespaces

for (c in colnames(d)) {
  d[[c]] = trimws(d[[c]])
}

# overwrite word length

d[['nl']] = nchar(d[['word']])

# check material

distnames = c ('distgramm','distword','distpseudo','distnw')

for (dty in distnames) {
   print(paste('checking', dty))
   chksame = d [ d[['word']] == d[[dty]], c('item','cond')]
   diffl = d[['nl']] != nchar(d[[dty]])
   if (nrow(chksame))  {
     print (paste('some distractors match targets in', dty, 'condition'))
     print (chksame)
   }
   if (any(diffl)) {
      print ('some distractor has different number of letters:')
      print (d[diffl, c('item', 'cond')])
   }
}

rm(list=ls())
source('makeLists.R')
q()
